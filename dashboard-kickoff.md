# 대시보드 착수 프롬프트

`docs/design/dashboard.md` 를 먼저 읽힌다.
데이터 탭은 M1 직후, 트레이딩 탭은 M3, 학습 탭은 M4에 만든다.
**셸(D-1)은 처음부터 3탭 구조로 만든다.** 나중에 탭을 끼워넣으면 전부 뜯어야 한다.

---

## D-1. 셸과 공통 헤더

```
docs/design/dashboard.md 를 읽고 lattice/dashboard/ 셸을 만들 계획을 세워줘.
아직 개별 탭 내용은 만들지 마. 뼈대만.

- Flask + ECharts CDN. 빌드 도구 없음
- 3탭 구조: 트레이딩 / 학습 / 데이터
- 공통 헤더: 브랜드 · 탭 · 타임머신 슬라이더 · as_of · 시스템 상태
- KPI 스트립 10칸 (한 줄, 모든 탭 공통)
- 탭 전환 시 as_of 유지
- 탭별로 API 엔드포인트를 분리해서, 안 보는 탭 데이터는 부르지 않게

규약:
- 모든 API 는 as_of 파라미터를 받는다. 예외 없음
- 임계치(12/22/30, IC 0.03, 반영률 30%)는 store.config 에서 읽는다.
  하드코딩 금지
- Jinja 는 뼈대만, 데이터는 전부 /api/* JSON
- localStorage / sessionStorage 금지
- ECharts 공통 테마를 파일 하나로 분리해줘 (색·폰트·축 스타일)

docs/design/fund-reference.html 을 열어보고 색·타이포·밀도를 맞춰줘.
그건 구현물이 아니라 스타일 기준이야.
```

---

## D-2. 데이터 탭 (M1 직후)

```
데이터 탭을 만들어줘.

표시: 소스별 커버리지, 결측률 추이, 지연 실측 분포(p50/p90),
universe 종목 수 추이, 정정공시 발생 건수, 최근 수집 실패,
LLM 월 비용, 캐시 적중률.

한 화면에 전부 들어가야 해. 스크롤 없이.
```

---

## D-3. 트레이딩 탭 (M3)

```
dashboard.md §4 대로 트레이딩 탭을 만들어줘.
12컬럼 그리드, 1920×1080 에서 스크롤 없이 들어가야 해.

배치:
- NAV vs 벤치마크 (+ 초과수익 서브차트)
- MDD 예산 게이지 / 오늘의 결정 + Claude 한 줄
- 언더워터 (12/22/30 밴드) / 자본 배분 / 에이전트 15점등
- 보유 포지션 테이블 (20일 스파크라인, 청산 소요일수 포함)
- 오늘의 후보 상위 15 / 섹터·상관 히트맵

시그니처 두 개를 특히 신경 써줘:

1. 언더워터 차트 — 배경에 12/22/30 밴드를 색으로 깔아.
   보상 함수를 그대로 시각화한 거야. 벤치마크 낙폭은 점선으로 겹쳐

2. MDD 예산 게이지 — 상태별로 문구가 바뀌어야 해:
   자유구간이면 "자유구간 · 페널티 없음 · 여유 3.7%p"
   리스크를 안 쓰고 있는 것도 문제라는 걸 화면이 말해야 해

액션 반영률이 30% 미만이면 경고색으로.

포지션·후보 행을 클릭하면 Decision Trace 모달이 떠야 해 (§7 형식).
별도 탭이 아니라 모달이야.

밀도 규칙(§8)을 지켜줘. 패널 헤더 28px, 테이블 행 26px,
애니메이션 없음, 숫자는 tabular-nums.
```

---

## D-4. 학습 탭 (M4)

```
dashboard.md §5 대로 학습 탭을 만들어줘.

배치:
- explained_variance 추이 (0.1 기준선) / 커리큘럼 C0~C5 진행도 + 오라클 카나리 통과 여부
- 학습 곡선 (에피소드 보상) / approx KL · entropy · gradient norm
- Analyst별 IC 60일 이동평균 (9줄 멀티라인 + 0.03 기준선)
- 베이스라인 대비 IR (벤치마크/동일가중/스코어 비례) / 시드 간 성과 분산 + policy churn
- 하이퍼파라미터 시행 테이블 (Optuna trial)

특히:
- explained_variance 패널에 0.1 기준선을 그어줘. 0 근처에 붙어 있으면
  학습 실패고, rl-training.md §3 의 6가지 원인 중 어디까지 배제했는지도 표시해줘
- Analyst IC 멀티라인에 0.03 기준선. 어느 Analyst가 가중치를 받고 있는지
  한눈에 보여야 해
- 베이스라인 대비 IR 패널이 이 탭의 결론이야. 스코어 비례를 못 이기면
  RL을 쓸 이유가 없다는 걸 항상 보여줘

한 화면에 스크롤 없이 들어가야 해.
```

---

## D-5. 밀도 점검

```
세 탭을 1920×1080 과 1440×900 에서 렌더해서 스크린샷을 보여줘.

확인:
- 스크롤 없이 들어가는가
- 빈 여백이 정보로 대체될 여지가 있는가
- 숫자 자릿수가 정렬돼 있는가
- 축 레이블·범례 중 지워도 되는 게 있는가

밀도가 부족한 패널이 있으면 스파크라인이나 보조 지표를 추가로 제안해줘.
```
