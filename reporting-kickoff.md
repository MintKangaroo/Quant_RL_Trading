# 리포트 · Gmail 착수 프롬프트

`docs/design/reporting.md` 를 먼저 읽힌다. **M3 이후**에 착수한다.
각 단계는 plan mode로 시작하고, 끝나면 커밋 후 `/clear`.

---

## R-0. LS_KR Gmail 세팅 조사

```
LS_KR 레포에서 Gmail 발송 관련 코드를 찾아 정리해줘. subagent를 써.
코드는 아직 쓰지 마.

확인할 것:
1. SMTP 연결·인증 방식 (앱 비밀번호인지 OAuth인지)
2. 자격증명이 어디에 저장돼 있나 — 코드에 하드코딩돼 있나?
3. 첨부 파일 처리, HTML 본문 렌더링 방식
4. 재시도·실패 로깅 로직
5. 발송 스케줄러가 어떻게 붙어 있나

그대로 이식 가능한 부분과 고쳐야 할 부분을 나눠서
docs/port-gmail.md 로 정리해줘. 파일 경로와 함께.

자격증명이 코드에 하드코딩돼 있으면 반드시 표시해줘.
Lattice 로 가져올 때는 .env 로 옮겨야 해.
```

---

## R-1. 리포트 데이터 계층 (LLM 없이)

```
docs/design/reporting.md 를 읽고 lattice/reporting/ 구현 계획을 세워줘.
이 단계에서는 Claude API를 쓰지 않아. 숫자만 모으는 계층이야.

build_report(kind, as_of) -> ReportData     kind: "close" | "weekly"

모든 수치는 store.get(as_of=...) 경유. 예외 없음.
ReportData 는 pydantic 모델로 정의하고 4개 섹션을 담아:

① 오늘의 매매
   체결 내역(종목·수량·체결가·슬리피지), 일간 손익, 초과수익,
   낙폭 예산 사용률, 목표 대비 실현 비중 괴리, 액션 반영률,
   차단된 종목과 사유

② 학습 현황
   오늘 학습 스텝 수, explained_variance 7일 추이,
   Analyst별 IC 60일 이동평균과 변화량, 재학습 트리거 발동 여부,
   ModelOps 판정, 커리큘럼 단계, 베이스라인 대비 IR

③ 뉴스 시황
   시장 전반 뉴스 3~5건, 보유·후보 종목 관련 뉴스,
   News Analyst 차단 건과 카테고리

④ 주식 시황
   KOSPI·KOSDAQ·S&P500·나스닥·원달러, 섹터별 등락,
   인기 주식(거래대금 z-score 상위 / 언급량 상위 / 신고가)
   ← 각 인기 종목이 Lattice 후보에 포함됐는지 여부를 반드시 함께 담아
   내일 예정 이벤트

weekly 는 여기에 주간 귀속(종목/섹터/Analyst/환율)과
다음 주 KR/US 배분 변경 여부를 추가.

결정론 테스트: 같은 as_of 로 두 번 만들면 동일한 ReportData 가 나와야 해.
```

---

## R-2. 이메일 렌더링

```
ReportData 를 이메일 HTML 로 렌더링하는 계층을 만들어줘.

제약 (reporting.md §3):
- 이메일 클라이언트는 JS를 실행하지 않아. ECharts 사용 불가
- 인라인 CSS 만 사용 (style 태그는 일부 클라이언트가 무시)
- 라이트 배경. 다크는 클라이언트 다크모드 반전으로 깨져.
  대시보드는 다크지만 메일은 라이트야 — 별개 스타일
- 차트는 서버에서 PNG 렌더 → 첨부 후 cid 참조.
  외부 링크 이미지는 차단되는 경우가 많아
- 차트는 NAV 곡선 / 언더워터 / explained_variance 추이 세 개까지
- 나머지는 표와 스파크라인
- Gmail 은 본문이 크면 잘라내니(clipping) 간결하게
- 상세는 대시보드 링크로. ?as_of=... 를 붙여 그 시점으로 열리게

인기 주식 섹션에는 "참고 정보 · 매매 후보 아님"을 명시하고,
후보 리스트와 시각적으로 분리해줘.

액션 반영률이 30% 미만이면 경고 배지를 붙여줘.

렌더 결과를 파일로 저장해서 브라우저에서 확인할 수 있게 해줘.
```

---

## R-3. Claude 요약 계층

```
reporting.md §4 대로 Claude API 요약 계층을 붙여줘.

- 입력은 ReportData 를 압축한 구조화 JSON 만.
  원본 뉴스 본문을 그대로 넣지 마 (인젝션 경로 + 비용 폭증)
- 출력 형식:
  {"headline": "...", "severity": "info|warn|alert",
   "sections": {"trading": "...", "learning": "...",
                "news": "...", "market": "..."}}
- 숫자는 Claude가 만들지 않아. 모든 수치는 store 값을 템플릿에 꽂아.
  Claude는 문장으로 엮기만 해
- 매수·매도 추천 문구 금지. 사실과 근거만
- agent_cache 에 저장. 같은 입력이면 캐시 반환, 재호출 금지
- 실패 시 템플릿 폴백 — Claude 없이 숫자 표만으로도 메일이 나가야 해

입력 상한: 후보 30 · 차단 10 · 인기 20 · 뉴스 20건.
월 토큰 비용을 agent_health 에 기록해줘.
```

---

## R-4. Gmail 발송

```
docs/port-gmail.md 를 참고해 LS_KR 의 Gmail 발송 코드를 이식해줘.
커밋 메시지에 출처를 남겨: port(reporting): LS_KR Gmail SMTP 이식

반드시 고칠 것:
- 자격증명을 .env 로 이동. 코드에 남은 건 제거하고 .env.example 에 자리만
- 앱 비밀번호 재발급 절차를 README 에 문서화

발송:
- 마감 리포트 = 국장 마감 후 / 주간 = 금요일 마감 후
- 미장 결과는 다음 날 마감 리포트에 포함. 새벽 발송 없음
- 휴장일에는 보내지 않음
- 하루 1~2통 제한. 종목별·이벤트별 발송 금지
- 실패는 재시도 후 로깅

중요: 리포트는 비필수 경로야.
생성·Claude 호출·SMTP 어느 것이 실패해도 Collector·Selector·Executor 는
영향받지 않아야 해. 예외가 전파되지 않도록 격리해줘.

reports 테이블에 본문·입력 스냅샷·수신자·발송시각을 저장해줘.
```

---

## R-5. 검증

```
리포트 전체를 검증하고 각 항목마다 실행 명령과 출력을 증거로 보여줘.

1. 과거 임의 날짜 20개로 재생성 → 두 번 실행 시 동일한지
2. Claude 호출을 강제 실패시켰을 때 템플릿 폴백으로 메일이 나가는지
3. SMTP 를 강제 실패시켰을 때 매매 경로가 멈추지 않는지
4. store 를 거치지 않고 데이터를 참조하는 지점이 있는지
5. 실제 메일을 본인 주소로 보내고 모바일·데스크톱 레이아웃 확인
6. .env 없이 실행했을 때 자격증명 누출 없이 안전하게 실패하는지
```
