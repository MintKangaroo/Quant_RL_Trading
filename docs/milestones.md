# 마일스톤

원칙: **M3까지는 RL 없이 돌아가야 한다.**
RL이 없으면 아무것도 안 되는 구조로 만들지 않는다. RL은 작동하는 시스템 위에 얹는 것이다.

각 마일스톤의 완료 기준이 전부 켜지기 전에는 다음으로 넘어가지 않는다.

---

## M1 — 데이터 창고 + 리플레이 엔진

에이전트 0명. 돈도 벌지 않는다.
**"과거 어느 시점이든 그때 알던 것만 정확히 재현"** 되는 상태를 만든다.

### 산출물
- `store/` — 이중시간 저장, 데이터 게이트
- `replay/` — Clock, 이벤트 로그, 체결 시뮬레이터
- `collectors/` — 시장 수집자, 문서 수집자
- Data Quality 화면
- `tests/invariants/` — 검증 테스트 4종

### 완료 기준
- [x] 과거 5년치 전종목 curated 백필 완료 (상장폐지 종목 포함)
- [x] 임의 과거 날짜 100개 리플레이 → 전부 결정론적
- [x] 결정론 / 미래 훔쳐보기 / 생존편향 / 정정공시 테스트 통과
- [x] `datetime.now()` 직접 호출 0건, 데이터 직접 조회 0건 (CI 검사)
- [x] 지연 실측 수집 시작 (p50/p90 대시보드 표시)

검증: `uv run python tools/verify_m1.py` — 다섯 항목을 실제 창고 위에서 확인하고
하나라도 실패하면 종료코드가 0이 아니다. 체크박스를 손으로 켜지 않는다.

2026-08-11 결과: KR 1,223 거래일 / 3,383,617행 / 커버리지 100% / 상장폐지 348종목,
리플레이 100일 바이트 일치(불일치 0), 수집 지연 p50 84ms · p90 1,069ms.

---

## M2 — Analyst 투입 + IC 검증

### 산출물
- `analysts/` — chart, flow_kr, flow_us, fundamental, news, sns, regime, event, risk
- `schemas/` — Signal, Verdict
- IC 측정 파이프라인 (purged K-fold + embargo)
- Agent Health 화면

### 완료 기준
- [x] 9개 Analyst 전부 구현, Signal 기록 중
- [x] 최소 2개 Analyst가 IC 0.03 통과 (OOS, 표본 200거래일 이상)
- [x] 국장/미장 IC 별도 측정 완료 — **미장은 chart·risk·regime 3개로 판정한다**
- [x] News·SNS 거부 성적표 집계 시작 — **파이프라인 가동을 기준으로 한다**
- [x] 통과하지 못한 Analyst는 가중치 0(관찰 모드)으로 동작

> 백테스트 IC가 0.15처럼 나오면 재능이 아니라 버그다. 먼저 누수를 의심할 것.

### 측정 결과 (2026-08-13, 300세션 · purged K-fold + embargo)

| Analyst | KR | US | 가중치 |
|---|---|---|---|
| **risk** | **+0.0777** | **+0.0585** | 1.0 |
| **fundamental** | **+0.0708** | — | 1.0 |
| **event** | **+0.0356** | — | 1.0 |
| chart | +0.0014 | +0.0145 | 0.0 |
| regime | −0.0143 | −0.0087 | 0.0 |
| flow_kr | −0.0011 | — | 0.0 |
| flow_us | 입력 0건 | 입력 0건 | 0.0 |

KR 2,787종목 · US 6,440종목. 통과 4개(risk 는 두 시장), 관찰 모드 5개.

**risk 가 두 시장에서 통과한 것이 이 마일스톤의 실질이다.** 한 시장에만
맞는 신호는 그 시장 표본에 사후적으로 맞춘 것일 수 있다. 시장을 바꿔도
남는 신호는 그 의심이 한 단계 줄어든다.

**chart 는 두 시장 모두 미달이다.** KR +0.0014 는 `low_volatility` 를 뺀 뒤의
정직한 숫자다 — 그 피처는 risk 의 것이었고, 그걸 넣은 채 잰 +0.0197 은 빌린
성적이었다. chart 고유 피처 여섯은 피처별 IC 에서 전부 잡음으로 나왔고,
가장 큰 가중치(25%)를 받은 momentum_20 의 IC 가 정확히 0.0000 이었다.

유력한 원인은 **타깃과 피처의 시간 축이 어긋난 것**이다. 타깃은 5일인데
(`ic.HORIZON_DAYS`) momentum_20·60 은 몇 달 단위 현상을 본다. 고칠 방향은
5일 축 피처로 교체하는 것이지 가중치 조정이 아니다.

**지금은 하지 않는다.** 표본이 KR 300세션 하나뿐이라, 새 피처를 열 개 시도해
하나가 통과하면 그건 신호가 아니라 열 번 던져 한 번 맞은 것일 수 있다. 실전
데이터가 쌓인 뒤 새 표본으로 본다.

regime·flow_kr 은 폴드 부호가 시기에 따라 뒤집힌다 — 약한 신호가 아니라
잡음이다.

**event 는 DART 공시 574,829건이 들어오며 피처가 2개 → 6개가 됐다**
(buyback·dividend·contract·dilution·distress + maturity). +0.0333 → +0.0356.
같이 드러난 것이 `no_halt` 였다 — 실측 상수(고유값 1개)라 IC 가 nan 인데
가중치의 45% 를 들고 있었다. 뺐고, `combine` 이 분산 0 인 피처를 자동으로
제외하게 했다.

**상수 피처를 빼도 IC 는 바뀌지 않는다** — IC 가 순위상관이고 상수 열은
점수를 상수배 할 뿐이라 순위가 그대로다(실측: 폴드까지 소수점 일치). 달라지는
것은 **점수의 크기**이고, 그건 M3 Selector 가 Analyst 들을 합칠 때 쓴다.

**미장 수치는 생존편향 위에 있다.** LS 가 상폐 종목 시세를 주지 않아
명단에 상폐 종목이 0건이다. 실제보다 후하게 나온다는 전제로 읽는다
(`collectors/us_universe_panel.py`).

### 판정 기준을 좁힌 두 곳 (2026-08-13 확정)

**미장 IC 는 3개만 잰다.** fundamental 은 미장 재무가 없고, event 는 미장
이벤트가 없고, flow_us 는 입력이 구조적으로 0건이다 — 미국에는 투자자별
순매수 공시 제도 자체가 없다(`analysts/flow_us.py` 모듈 docstring). 없는
데이터로 잰 IC 는 "안 먹혔다" 가 아니라 **"잰 게 없다"** 이므로, 재지 않는
것이 재서 0 을 적는 것보다 정직하다. 붙일 자리(short interest·13F·ETF
자금흐름)는 남겨 뒀고, 그건 M2 가 아니라 별도 수집기 작업이다.

**News·SNS 성적표는 "파이프라인 가동" 을 집계 시작으로 본다.** 배관은 끝까지
검증됐다 — 뉴스 40건 → 패턴 4건 → LLM 4건 전부 기각. `verdicts` 0건은
고장이 아니라 **거부할 사유가 없었다는 정답**이다. 실제 차단 기록이 쌓일
때까지 기다리는 기준은, 나쁜 뉴스가 나야만 마일스톤이 닫히는 이상한 조건이
된다. 대신 **거부가 처음 기록되는 날 배관을 한 번 더 확인**한다 — 0건이
정답인 구간에서는 고장과 정답이 같은 모습이기 때문이다.

---

## M3 — Selector + Executor (RL 없이 실전 가능)

**여기서 이미 돈을 벌 수 있어야 한다.**

### 산출물
- `selector/` — 후보 선정, Analyst 가중치 진화
- `executor/` — 주문 변환, 상한, 리스크 가드, 킬스위치
- Allocator 자리에는 룰 기반 베이스라인 (동일가중 또는 스코어 비례)
- Fund 화면, Decision Trace 화면

### 완료 기준
- [ ] shadow 10거래일 무사고
- [ ] OOS 백테스트 MDD 20% 이내
- [x] 킬스위치 실제 발동 테스트 통과 — `tests/executor/test_executor.py`
- [ ] 실전 소액 투입 (배선 검증 목적)
- [ ] 슬리피지 실측이 모델 예측의 ±30% 이내

### 검증 (2026-08-15 추가)

`uv run python tools/verify_m3.py` — 위 다섯 항목을 실제 창고 위에서 판정한다.
**체크박스를 손으로 켜지 않는다** (`verify_m1.py` 와 같은 규약).

M1 검증기와 다른 점은 상태가 셋이라는 것이다 — `PASS` · `FAIL` · **`미측정`**.
지금 다섯 중 넷이 미측정인데, 그건 고장이 아니라 **아직 잴 수 없다**는 뜻이다.
FAIL 로 적으면 고장으로 읽히고 PASS 로 적으면 거짓이 되므로 갈라 놓는다.
종료코드도 셋이다: 0 전부 통과 · 1 FAIL 있음 · 2 FAIL 없이 미측정만.

함정 둘을 판정에 박아 뒀다.
- **매매 0건이면 MDD 는 언제나 0%** 다. 완벽한 성적이 아니라 아무것도 안 한
  것이므로 미측정으로 뺀다. NAV 가 초기값에 고정돼 있으면 shadow 도 마찬가지다.
- **시뮬레이션 체결로는 슬리피지 기준을 통과할 수 없다.** `replay/fills.py` 가
  모델 그 자체로 만든 체결이라 그것으로 모델을 검증하면 정의상 오차 0 이다.
  `trades.source == "broker"` 만 자격이 있다 (`tools/measure_slippage.py`).

### 실매매 전에 반드시 있어야 하는데 **아직 없는 것** (2026-08-14 확인)

1. ~~**주문 전송 경로가 없다.**~~ **2026-08-15 배선했다.** `broker/` —
   LSBroker·fills·lifecycle 이 붙었고 파이프라인에 배선됐다(아래 진행 표
   "3-3 주문 전송"). 호가단위도 `executor/ticks.py` 로 들어왔다 — 없으면
   지정가가 전량 거부된다("3-3 호가단위").

   아래는 그 배선 전의 기록이다. `executor/` 는 목표비중을 주문으로 바꿔
   창고에 적는 데서 끝났다. `place_order`·`submit_order`·`CSPAT*` 호출이
   한 군데도 없었다. `ls_client.py` 에 `PATH_ORDER` 와 체결 응답코드
   (00039·00040·00463)가 LS_KR 에서 이식돼 있었지만 부르는 함수가 없었다 —
   바닥재만 깔린 상태였다. 부분체결·재호가·취소·멱등 client order id 가
   전부 거기에 딸려 있었다.
2. **모의투자로는 우회할 수 없다.** `ls_client.py:81` — paper 모드에서 잔고
   (t0424)와 주문(CSPAT*)은 LS 가 막는다. 주문 배선 검증에는 실전 appkey 가
   필요하다.
3. **shadow 가 체결 0 이다.** 2026-08-13·14 이틀 다 후보 24 · 주문 12 ·
   체결 0 이고 NAV 가 10,000,000 에 고정돼 있다. 무사고이긴 하지만 **손익
   경로가 한 번도 안 돌았다** — 날짜만 채우면 10일이 차도 검증이 아니다.
4. ~~**벤치마크가 계산되지 않는다.**~~ **2026-08-15 배선했다.**
   `backtest/loop.py` 가 NAV 스냅샷과 **같은 as_of · 같은 환율**로
   `accounting/benchmark.py:level()` 을 부른다. `snapshot.write` 는 값을 안
   받으면 스스로 계산한다 — 호출부에만 맡기면 한 곳만 빠져도 그 경로가 통째로
   null 이 되는 것이 이 결함 그 자체였으므로 구조로 막았다. 결측일은 앞 값으로
   안 채우고(낙폭이 지워진다) 이유를 `nav_daily.benchmark_note` 에 남긴다.
   휴장과 구멍은 날짜 수가 아니라 **그 시장의 거래일 달력**으로 가른다 —
   날짜로 재면 설 연휴가 구멍이 되고 3일짜리 수집 실패가 휴장이 된다.
   지수는 ~~창고에 실제로 있는 `KR:IDX:KRX 300`~~ **(2026-08-15 갱신)**
   `KR:IDX:KOSPI` · `US:IDX:SP500` 을 쓴다 — 대표지수가 KRX Open API 의
   시장별 경로(`/idx/kospi_dd_trd`)에만 있어서 그동안 수집이 안 됐을 뿐이고,
   지금은 2020-08 부터 들어와 있다(`config/quant_rl_trading.yaml` 참고).
   **코스피라고 부르지 않는다** 는 원칙은 그대로였고, 그래서 이름이 들어오자
   대용치(KRX 300)를 걷어냈다 — 대용치 바꿔치기가 더 큰 거짓말이다.

   아래 내용은 그 배선 전의 기록이다. `accounting.nav.blended_benchmark()` 는
   구현돼 있으나 **어디에서도 호출되지 않아** `benchmark_index` 가 실전·데모·
   테스트 픽스처 전부 null 이었다. 초과수익도 벤치마크 낙폭도 근거가 없었다.
   config 는 `KOSPI_TR`·`SPX_TR`(총수익지수)을 요구하는데 창고에는 가격지수
   (KRX 100·KRX 300·SP500)뿐이고, 가격지수를 쓰면 배당수익률만큼 가짜 알파가
   생긴다.

   **2026-08-14 조사 결과 — TR 은 지금 키로 못 받는다.** 세 경로를 실제
   네트워크 호출로 확인했다. KRX Open API 는 지수 129개 전수에 TR 이 하나도
   없고 배당수익률 필드도 없다. LS `t1511`(경로가 `/indtp/market-data` 다 —
   문서에 없어 실측으로 알아냈다)은 전부 가격지수 필드. FRED 는 S&P 500 TR 을
   무료 재배포하지 않는다(S&P DJI 라이선스). **셋 다 배당수익률조차 없어
   `PR + 배당수익률` 근사도 막혔다.** 진짜 TR 은 KRX 유료 지수 라이선스나
   Bloomberg/Refinitiv 가 필요하다 — 별도 계약 사항이다.
   가격지수로 가기로 정한다면 config 주석 · `blended_benchmark` 독스트링 ·
   `nav_daily.benchmark_index` 컬럼 설명 · 화면 배지 **네 곳 모두**에
   "배당 미반영 — 우리에게 유리하게 나온다" 를 박아야 한다.
5. **섹터 상한이 걸리지 않는다 — 지금은 의도적으로 그렇다.** 수집은
   붙였으나(`sectors` 테이블) KRX 일별매매의 `SECT_TP_NM` 은 업종이 아니라
   **KOSDAQ 소속부**다 — KOSPI 는 942 종목 전부 빈 문자열이고 KOSDAQ 만
   우량기업부·벤처기업부 등으로 갈린다. 그걸로 건 상한은 상관 분산이 아니라
   시장 등급 분산이라, 켜면 "분산되고 있다" 는 착시만 생긴다.

   ~~진짜 업종 분류(KRX 업종분류현황)를 받기 전까지 끈다.~~ **2026-08-15
   확인 — KRX 정식 Open API 에는 업종분류 상품 자체가 없다.** 종목기본정보
   3개 엔드포인트(유가증권·코스닥·코넥스)를 실제로 호출해 확인했고, 셋 다
   `SECT_TP_NM` 만 준다. `data.krx.co.kr` 의 업종분류현황 화면은 OTP+CSV
   다운로드 경로뿐이라 이 프로젝트가 이미 금지한 `pykrx` 스크래핑과 같은
   문제(약관 위반·IP 차단 전례)를 안고 있어 쓰지 않는다. 대신 **DART
   `company.json` 의 `induty_code`(표준산업분류)로 받았다** —
   `source=dart_company` 로 KR 2,761/2,874종목(96.1%) 적재 완료
   (`collectors/dart_sectors.py`, `tools/collect_sectors.py`). `sectors`
   자연키가 `(entity_id, valid_from, source)` 로 넓어져 KRX 소속부와
   공존한다. **다만 섹터 상한은 여전히 꺼져 있다** — 데이터가 들어온 것과
   상한을 켤지는 별개 판단이라, 커버리지를 본 뒤 따로 결정한다
   (`selector/pipeline.py` 에 훅은 파여 있다).
6. **`executor/supervise.py` 는 호출부가 없다.** `register`·`step`·
   `cumulative_from_sync` 를 부르는 프로덕션 코드가 0건이다(재수출뿐이라
   `find_dead_code.py` 도 못 잡았다). 미체결 재호가·최대 재시도 취소·부분체결
   잔량 재호가 — 이 모듈이 판단은 하지만 그 판단을 실행에 옮기는 곳이
   없다. **지금은 무해하다** — 백테스트·shadow 둘 다 `PaperBroker` 를 쓰는데
   `register()` 는 `broker_order_no` 없는 주문(paper)을 자동으로 걸러서
   아무것도 등록하지 않는다(`tests/executor/test_supervise.py::
   test_register_skips_paper_acks`). 그래서 지금 당장의 성적에는 영향이
   없지만, **`LSBroker`(실전 appkey)가 붙는 순간부터는 이 배선 없이는
   재호가·취소가 실제로 나가지 않는다.** 실계좌 소액 투입과 같은 작업으로
   묶는다 — `LSBroker` 가 있어야 "진짜 도는지" 를 유닛 테스트 너머로 증명할
   수 있다(2026-08-15 조사, `docs/live-order-checklist.md` 에 단계 추가함).
   장중을 반복해서 도는 루프 자체가 지금 이 저장소에 없다는 것도 같이
   확인했다 — `tools/run_session.py` 는 cron 으로 한 번 뜨고 끝난다.
   `docs/runbook.md` §7 은 "워커 프로세스는 상시 실행" 을 이미 전제하고
   있으니, 배선할 때는 그 모양(상시 워커)으로 가는 것이 맞다.

### 진행 (2026-08-14)

| 단계 | 상태 | 커밋 |
|---|---|---|
| 3-1 회계 계층 | ✅ | `fe5b243` |
| 3-2 Selector | ✅ | `a6fa31d` |
| 3-3 Executor | ✅ | `f4a3a88` |
| 3-4 룰 베이스라인 | ✅ | `9694e22` |
| 3-4 가중치 진화 | ⏸ | Session 뒤로 |
| 3-5 Session | ✅ | `a7b430b` |
| 3-4 가중치 진화 | ⏸ | 속도가 먼저다 (아래) |
| 3-6 백테스트 루프 | ✅ | `db4a59b` |
| 3-6 워크포워드 | ✅ | `b7a9907` · `cde8e12` |
| 3-6 트레이딩 탭 | ✅ | `7e41ed4` · `453ed2c` |
| 3-7 shadow 배선 | ✅ | `cfebd3c` · 크론 평일 16:30 |
| 3-7 shadow 10거래일 | **0/10 (재시작)** | 체결이 한 번도 안 됐다 — 아래 참조 |
| 3-3 주문 전송 | ✅ | `broker/` — LSBroker·fills·lifecycle, 파이프라인 배선 |
| 3-3 호가단위 | ✅ | `executor/ticks.py` — 없으면 지정가가 전량 거부된다 |

**shadow 카운트를 0 으로 되돌렸다 (2026-08-14).** 8/13·14 이틀은 후보 24 ·
주문 12 · **체결 0** 이었고 NAV 가 10,000,000 에 고정돼 있었다. "아직 D+1 이
안 왔다" 가 아니라 **고장이었다** — `tools/run_session.py` 가 하루씩
`loop.run(start=day, end=day, warmup_days=0)` 을 부르는데, `previous_session`
이 그 호출 안에서만 살아서 유일한 이터레이션에서는 언제나 `None` 이다. 그래서
D+1 체결 단계(`backtest/execution.py`)가 **한 번도 호출되지 않았다.** 몇 달을
돌려도 영원히 0 이었을 것이다. 지나간 이틀은 사고가 날 코드를 지나가지
않았으므로 무사고가 아니라 **미검증**이다.

**백테스트 루프를 붙이며 드러난 것 두 가지.**

1. **`fx` 테이블이 0행이었다.** 회계는 환율 없이 NAV 계산을 거부하므로(그게
   옳다) 그동안 회계는 테스트 위에서만 돌고 있었다. FRED DEXKOUS 로 2021-01 ~
   2026-08 1,400행을 백필했다 (`tools/collect_fx.py`)
2. **워밍업 없이는 매매가 0건이다.** confidence 가 60거래일 롤링 IC 라
   신호 이력이 없으면 0 이고, 0 이면 합성 점수의 분모가 0 이 되어 후보가 통째로
   비어 버린다. 성적표는 평평한 직선이 나오는데 그건 "안 샀다" 가 아니라
   **"아직 잴 수 없었다"** 이다

**하루당 198초** (2026-08-14 실측). 처음 잰 값은 432초였고, 파티션 하한
프루닝을 켜서 절반으로 줄였다 — `observation_lag_days` 를 선언한 테이블이 둘뿐이라
`lookback` 을 줘도 5년 파티션을 전부 열고 있었다(`71e4316`). 대시보드도 같은
원인으로 14.7초 → 2.4초가 됐다.

남은 198초 중 **190초가 Analyst 계산**이다. 가중치 진화(개체 64 × 세대 40)와
M4 학습은 여기를 더 줄여야 가능하다.

### 이 구간에서 잡은 결함 — 셋 다 증상이 "후보 0건" 이었다

1. **confidence 가 못 잰 것을 0 으로 두고 있었다.** 합성이
   `Σ(w·score·conf)/Σ(w·conf)` 라 분모가 0 이 되어 후보가 통째로 빈다.
   신호 13,746행이 쌓였는데 아무것도 못 골랐다 (`0409951`)
2. **세션 시각(15:40)이 신호 공표(16:00)보다 일렀다.** 결정이 언제나 전날
   신호로 내려가 하루가 통째로 밀렸다 (`cfebd3c`)
3. **장중 실행이 미래 시각 주문을 만들었다.** 그 주문은 어떤 조회에서도
   안 보인다 — 창고에는 있는데 화면에는 없는, 가장 헷갈리는 고장이다

**진화를 Session 뒤로 미룬 것은 의도다.** selector.md §3 이 "평가는 실제
백테스트로 한다. 점수 상관만으로 평가하면 실제로 못 사는 종목으로 적합도를
얻는다" 고 못 박았고, 백테스트 루프는 Session 위에서만 돈다. 순서를 지키면
설계가 금지한 방식으로 만들게 된다.

---

## M3.5 — 리포트·이메일 (M4와 병행 가능)

`docs/design/reporting.md` · 프롬프트는 `reporting-kickoff.md`.
보고할 매매가 있어야 의미가 있으므로 M3 이후에 착수한다.

### 완료 기준
- [ ] 마감 리포트(매매·학습·뉴스시황·주식시황 4섹션) / 주간 리포트 Gmail 발송
- [ ] 과거 as_of 재생성 시 원본과 동일 (결정론)
- [ ] Claude 호출 실패 시 템플릿 폴백 동작
- [ ] LS_KR Gmail 세팅 이식 완료, 자격증명은 .env 로 이동
- [ ] 리포트 실패가 매매 경로에 전파되지 않음

---

## M4 — Allocator 투입

절차는 `docs/design/rl-training.md`, 프롬프트는 `M4-kickoff.md`.
**오라클 카나리 테스트를 통과하기 전에는 실제 학습을 시작하지 않는다.**

### 산출물
- `allocator/` — 상태 인코더(set encoder), 정책, 학습 루프
- 보상 함수 구현 (`reward-and-risk.md` 그대로)
- Model Lab 화면

### 완료 기준
- [ ] explained_variance가 유의미하게 0에서 벗어남
- [ ] **액션 반영률 30% 이상** — 미만이면 RL이 아니라 룰 시스템이다
- [ ] 실현 비중 되먹임 동작 확인
- [ ] 룰 베이스라인 대비 shadow IR 우위
- [ ] shadow 20거래일 + 사람 승인 후 실전 승격

> M4에서 학습이 안 되면 재정식화를 반복하지 않는다.
> 3회 시도 후에도 explained_variance가 0 근처면 **M3 룰 베이스라인으로 되돌리고**
> 원인을 분리해 다시 접근한다. 선행 프로젝트는 9차까지 끌고 가다 폐기했다.

---

## M5 — Auditor + ModelOps + Claude 리뷰

### 산출물
- `auditor/` — 귀속 분석
- `modelops/` — 재학습 판정, 드리프트 감지, IC 감쇠 감시
- Claude API 리뷰 파이프라인 + `reviews` 테이블
- Attribution, Risk, Regime 화면

### 완료 기준
- [ ] 일일 리뷰 자동 생성, headline이 Fund 화면에 표시
- [ ] 리뷰 결과가 `agent_cache` 에 저장되어 리플레이 시 재호출 없음
- [ ] Analyst별 성과 귀속이 가중치 진화에 피드백
- [ ] LLM 월 비용이 예산 내

---

## 중단 기준

각 마일스톤에 시한을 둔다. 시한을 넘기면 접거나 방향을 바꾼다.

- M4에서 RL 재정식화 **3회 실패** → M3 룰 베이스라인 유지, RL은 별도 트랙으로 분리
- Analyst가 6개월간 하나도 IC 0.03을 못 넘김 → 피처·타깃 설계를 원점 재검토
- 실전 12개월간 shadow IR이 지속적으로 음수 → 프로젝트 종료 검토

선행 프로젝트가 실패한 결정적 이유 중 하나는 **중단 기준이 없었다는 것**이다.
