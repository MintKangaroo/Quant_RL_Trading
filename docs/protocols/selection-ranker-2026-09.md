# 사전등록 — 시행 M: ranker 점수 위의 선정 (상위 N · 완충 · 시총가중 10% 상한) — 2026-09-04 · family `selection` · 1회

## 왜
4회차(drl-round4)가 닫히며 남은 레버는 "얼마씩" 이 아니라 "무엇을·몇 개를" 이다. 알파의 주인이 ranker 로 바뀌었으니
(시행 L) 선정 규칙(상위 24 · 퇴출 순위 48 밖 · 동일가중)을 그 점수 위에서 다시 잰다. 2b 의 교훈(시총가중 24 =
하이닉스 62%)대로 시총가중엔 **종목 상한 10%** 를 건다.

## 고정
- 데이터: 국장, `data/_diag/tradable-KR.pkl` 명단(2025-05-21~), 판정 세션 2025-05-21 ~ 2026-06-30(홀드아웃 봉인).
  점수 = 창고 `signals` 의 `ranker`(워크포워드 모델 열, 세션 as_of 관측)와 `risk`. 위험 하한 컷 = `selector.risk_floor_percentile`.
  시총 = `market_stats.market_cap`. 보유수익 = t+1 종가 → t+2 종가(결정 다음날 시가 근사), |일수익| > 50% 제외.
  비용 = 편도 0.41% × 회전. 지수 = KOSPI200.
- 변형 (전부 미리 고정, 6개):
  EW24(대조·현행) · EW36 · EW48 — 상위 N 진입, 보유 종목은 순위 ≤ 2N 이면 유지(최대 N), 동일가중.
  CW24 · CW36 — 같은 집합을 시총가중하되 종목 상한 10%(초과분 비례 재분배, 5회 반복).
  EWALL — 명단 전체 동일가중(참고; 기준 아님).
- 지표: 연수익·변동성·샤프·MDD·연회전·IR(vs K200)·**일별 (변형 − EW24) 차이의 NW t(lag 4)**.

## 채택 기준 (측정 전 고정)
변형이 채택되려면 셋 다: ① Δ일수익 NW t ≥ 2.0 ② MDD 가 EW24 보다 3%p 넘게 나쁘지 않을 것 ③ 연회전 ≤ EW24 × 1.5.
통과 변형이 여럿이면 순샤프 최고 하나. 없으면 **기각 — 현행 24·48·동일가중 유지**. 강세장 표본 꼬리표는 2b 와 같다.

## 채택 시
`selector` 설정(`selector.top_n` / 완충 배수 / 가중 방식)을 config 정정본으로 바꾼다 — 코드가 아니라 설정이다.
시총가중 채택이면 `portfolio-construction.md` 에 상한 규칙을 적고 배선한다.

## 결과 (2026-09-04 15:0x, 해시 e036f78e0e7196ed, research_trials `selection/M`) — **기각. 현행 EW24 유지.**

271세션(2025-05-21~2026-06-30), 위험 하한 0.20, 비용 후.

| 변형 | 연수익 | 변동 | 샤프 | MDD | IR(K200) | 연회전 | Δ vs EW24 NW t |
|---|---|---|---|---|---|---|---|
| EW24(현행) | +5.2% | 21.0% | +0.25 | −19.2% | −3.65 | 64.5 | — |
| EW36 | +4.5% | 21.0% | +0.21 | −19.9% | −3.77 | 55.4 | −0.18 |
| EW48 | +8.5% | 21.9% | +0.39 | −19.9% | −3.65 | 49.1 | +0.58 |
| CW24(10% 상한) | +11.3% | 22.9% | +0.50 | −20.7% | −3.48 | 71.3 | +1.00 |
| CW36(10% 상한) | +11.0% | 23.9% | +0.46 | −20.2% | −3.64 | 62.5 | +0.75 |
| EWALL(참고) | +2.8% | 23.7% | +0.12 | −28.5% | −4.08 | 11.8 | |

NW t 가 전부 2 미만이라 등록 기준 ①에서 탈락. 시총가중 10% 상한(CW24)이 방향은 맞지만(+6.1%p, t 1.0) 271세션으로는 못 가른다.

**이 표가 진짜로 말하는 것은 회전이다.** EW24 의 연회전 64.5 = 매일 포트의 약 26% 가 바뀐다는 뜻이고, 편도 0.41% 면
연 약 26%p 가 비용으로 나간다 — ranker 점수는 일별로 순위가 크게 흔들려 "순위 ≤ 48 유지" 완충이 거의 못 잡는다.
어제 국장 shadow(보유 14 중 9 퇴출)와 오늘 미장(15 중 14 퇴출)이 같은 현상이다. 다음 등록은 N 이 아니라
**점수 평활(예: 5세션 EMA)과 완충 확대(3N·체류 하한)로 회전을 줄이는 것**이다 — 아래 진단(기록만)이 그 근거.

### 진단 (기록만 — 시행 아님, 같은 271세션) — 회전이 알파를 먹는다

| 변형 | 비용전 연수익 · 샤프 | 비용후 연수익 · 샤프 | MDD | 연회전 |
|---|---|---|---|---|
| EW24 완충2N(현행) | +31.7% · 1.51 | +5.2% · 0.25 | −19.2% | 64.5 |
| EW24 완충3N | +28.2% · 1.35 | +10.8% · 0.52 | −17.6% | 42.2 |
| EMA3 완충2N / 3N | +23.5% · 1.11 / +21.0% · 0.98 | +9.2% · 0.44 / +10.8% · 0.50 | −19.0% / −20.9% | 34.7 / 24.8 |
| EMA5 완충2N / 3N | +20.5% · 0.96 / +25.2% · 1.16 | +9.4% · 0.44 / **+16.2% · 0.74** | −20.5% / −17.7% | 27.2 / 21.9 |
| EMA10 완충2N / 3N | +21.2% · 1.02 / +20.1% · 0.98 | +12.4% · 0.60 / +13.5% · 0.66 | −18.7% / −18.7% | 21.4 / 16.2 |

비용 전 +31.7%(샤프 1.5)가 비용 후 +5.2% — ranker 의 알파는 있는데 **회전 비용이 5/6 을 먹는다.** 평활(EMA)과
완충 확대는 비용 전 수익을 조금 깎고 회전을 1/3 로 줄여 비용 후를 두세 배로 만든다. 여덟 변형을 **보고 고른** 표라
이 자체는 채택 근거가 아니다 — 다음 등록(시행 N)은 여기서 **안 본 구간**(2024-03~2025-05, ranker 이력이 있는
앞쪽)에서 EMA5·완충3N 하나만 잰다.
