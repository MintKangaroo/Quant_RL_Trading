# 사전등록 — 비선형 랭커 변형 B (2026-08-27, 사용자 요청 재시도)

첫 시행(ranker-2026-08-27.md)의 실패 원인 둘을 겨냥한 **다른 가설**이다. 시행 1회.

## 바뀐 것 (둘 다 첫 시행의 진단에서 나옴)
1. **피처를 fundamental 7개로 제한** — 첫 모델은 low_volatility·maturity·dilution 을 외웠고 그것들이
   다음 20세션에서 0 이었다. 지속성이 확인된 축(재무)만 준다.
2. **학습창을 확장창(expanding)으로** — 최소 150세션에서 시작해 예측 블록마다 늘린다. 120세션
   고정창은 레짐 하나를 외운다.
3. 모델을 더 얕게: num_leaves 7 · min_data_in_leaf 500 · learning_rate 0.03 · 300 라운드 · feature_fraction 1.0.
   나머지(bagging 0.8, lambda_l2 1.0, seed 0) 동일. 타깃·퍼지·블록 길이(h5 z · 5 · 20) 동일.

## 대조군 둘
- fundamental 점수(현행) — 채택 기준의 대조.
- 같은 7피처의 **선형 결합**(학습창에서 최소제곱, 참고용) — 비선형이 선형보다 나은지 따로 본다.

## 채택 기준 (첫 시행과 동일)
1. ΔIC(h5) vs fundamental 의 NW t ≥ 2.0 · 2. 상위 24 h5 수익 ≥ 대조 · 3. 최악 블록 Δ ≥ −0.03.
기각이면 랭커는 이번 분기에 끝. 다음 분기 재시도는 새 데이터(홀드아웃 밖 신규 세션)가 쌓인 뒤.

---
## 결과 (2026-08-27, 해시 b7af71d3af6f1e68) — **기각**

판정 100세션(5블록, 2026-01~05). IC 랭커 **+0.018**(t 1.3) 대 fundamental **+0.081**(t 4.8), ΔIC −0.063
(NW t −4.05). 같은 7피처의 **선형 결합도 +0.027** — 학습된 가중은 선형이든 비선형이든 손으로 부호를
정한 fundamental 합성점수의 1/3 이다. 5블록 전부 열세(최악 −0.107). 상위 24 수익도 열세.

읽는 법: 이 표본(학습창 150~230세션)에서 타깃 z 는 잡음이 커서, **데이터로 가중을 배우는 것 자체가
사전 부호(prior)보다 못하다.** 첫 시행의 문제가 "피처 선택" 만이 아니었다는 뜻이다. 랭커는 이번 분기
종료. 다시 하려면 (a) 표본이 지금의 2배 이상 쌓이거나 (b) 타깃을 h20 처럼 덜 시끄러운 지평으로 바꾼
**새 가설**로만 — 그것도 사전등록 후 1회.
