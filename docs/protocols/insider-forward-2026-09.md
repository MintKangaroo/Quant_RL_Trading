# 사전등록 — 시행 AR: 내부자 거래 재료, 새 창에서 상위 24 로 (2026-09-24 등록 · 판정 = 홀드아웃 11월 개봉 · family `ranker` · 1회)

## 왜

사용자 지시(2026-09-24 "이것도 다시 재보자"). 6차 G4(국장 DART 임원 지분)·G7(미장 SEC Form 4)은 ΔIC ≈ 0(±0.0004)이라 기준 ① 로 기각됐지만
**상위 24 z-수익이 두 시장·세 시드 모두 올랐다**(국장 +0.04/+0.08/+0.07, 미장 +0.06/+0.10/+0.08). 소수 종목에만 있는 신호라 전 종목 평균인
IC 로는 안 보이는 모양이다. 그 창(2023-02~2026-06)은 이미 봤다 — **새 창에서, 상위 24 기준으로** 다시 잰다.

## 창 — 홀드아웃 금고, 11월 개봉 때 한 번

시행 AQ 와 같다: 판정 창 **2026-07-01 ~ 2026-11-13**, 판정은 **2026-11-23 이후**(금고 개봉 = modelops-ranker.md ③ 과 같은 한 번).

## 모델 — 지금 얼린다

- 6차와 같은 패널·피처·하이퍼파라미터(잎 7 · min_data 2,000 · lr 0.03 · 300 라운드 · rank-gauss h5, 두 시장 한 모델 + is_us).
  학습 자료 = **2026-06-30 까지**(퍼지 5). {대조 = 6차 대조 피처, 처리 = 대조 + G4 + G7} × 시드 0·1·2 = 6개.
  G4 는 국장만·G7 은 미장만 값이 있다(없는 시장은 0 — 6차 규칙). 두 묶음을 **한 처리로** 묶는다 — 같은 가설(내부자가 먼저 안다)의 두 시장 자료다.
- `tools/trial_insider_forward.py --freeze` 가 학습·저장하고 해시를 아래에 적는다. 판정 때 모델을 다시 학습하지 않는다.

## 포트 (실전과 같게)

점수 EMA5 · 상위 24 동일가중 · 완충 3N · **10세션 재조정(AO)** · 편도 0.41% · 수익 t+1→t+2.

## 채택 기준 — **셋 다, 두 시장 각각** (측정 전 고정)

1. 세 시드 평균 비용 후 연수익: 처리 > 대조.
2. **세 시드 모두** 같은 시드 대조보다 높다(3/3) — 6차에서 본 모양이 재현되나.
3. 처리 ΔIC(h5, 세 시드 평균) ≥ −0.01 — IC 를 깎는 재료가 아니다(G2·G5 가 그랬다).

두 시장 다 → 채택 후보(랭커 피처 추가 → modelops-ranker.md 재학습 게이트 → shadow). 한 시장만 → 그 시장만 후보, 사용자 결정.
**기록**: 상위 24 겹침(처리 vs 대조), 내부자 피처가 0 이 아닌 종목이 상위 24 에 든 비율, 시드별 표, MDD·회전.

## 한계

- 창이 약 95세션·재조정 약 10번이다. 그래서 ② 3/3 을 둔다(우연히 셋 다 같은 부호일 확률 1/4, 두 시장 다 1/16 — 다만 시드는 서로 독립이 아니다).
- 국장 G4 자료는 2024-08~ 라 학습 표본이 짧다(6차와 같은 조건).

## 모델 해시 (--freeze 뒤 적는다)

(비어 있음)
