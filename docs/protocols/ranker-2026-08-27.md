# 사전등록 — 비선형 랭커 (LightGBM) 워크포워드 1회 (2026-08-27)

**시행 1회.** 하이퍼파라미터를 이 파일에 고정한다. 결과를 보고 바꾸면 그건 새 시행이다.

## 데이터
- `data/_diag/features-*-KR.pkl` (KR 300세션, 2025-05-21~2026-08-11, 각 세션 as_of 로 산출)
- 판정 세션은 **2026-06-30 이전만** (홀드아웃 제외). 유니버스 `tradable-KR.pkl`.
- 타깃: `targets-KR-h5.pkl` (5일 앞 수익의 횡단면 z, 진입 지연 1일).

## 피처 (13개, feature-registry 채택분 — 2026-08-22 결정, 여기서 바꾸지 않는다)
fundamental: roe, operating_margin, earnings_yield, profit_growth, book_to_market, sales_to_price, revenue_growth
event: dilution, maturity, dividend, buyback · risk: low_volatility · volume: volume_surge
세션별 횡단면 z 로 표준화. 결측은 0(중립).

## 모델 (고정)
LGBMRegressor: num_leaves 15 · n_estimators 200 · learning_rate 0.05 · min_child_samples 200 ·
subsample 0.8 (freq 1) · colsample_bytree 0.8 · reg_lambda 1.0 · seed 0. 피처 단조성 제약 없음.

## 워크포워드
학습창 120세션 → 다음 20세션 예측 → 20세션 전진 (purge: 학습창 끝과 예측 시작 사이 5세션 비움).
예측 세션 합집합이 판정 표본이다. 같은 세션에서 대조군은 `fundamental` 점수(현행).

## 채택 기준
1. ΔIC(h5) = IC(랭커) − IC(fundamental) 의 일별 차이, NW t ≥ 2.0
2. 상위 24 동일가중 h5 평균 초과수익이 대조 이상
3. 어느 한 월(20세션 블록)도 랭커 IC 가 대조보다 0.03 이상 낮지 않을 것 (꼬리 안정성)
셋 다 통과해야 채택. 통과하면 `selector` 에 `ranker` Analyst 로 배선하는 설계를 쓴다(다음 단계, 별도 시행).
기각이면 기록만 남기고 다음 분기까지 다시 시도하지 않는다.

---
## 결과 (2026-08-27 19:0x, 사전등록 해시 ac309d378fa430f1) — **기각**

판정 140세션(7블록, 2025-11-24~2026-06). IC 랭커 **−0.0006**(t −0.04) 대 fundamental **+0.0769**(t +5.17),
ΔIC −0.0774(NW t −4.66). 상위 24 h5 z-수익 랭커 −0.052 대 대조 +0.004. 7블록 전부 랭커가 낮고
최악 블록 Δ −0.193. 기준 셋 모두 미달.

마지막 모델의 gain 상위는 low_volatility·maturity·dilution·volume_surge — 학습창 안에서 강한
피처가 다음 20세션에서는 예측력이 0 이었다. 120세션 학습창은 이 피처들의 레짐 의존성을
외운다. 다음 분기 전에는 다시 시도하지 않는다(사전등록의 약속). 다시 할 때는 (a) 학습창을
늘리고 (b) fundamental 7개만으로 제한한 변형을 **별도 사전등록**한다.
