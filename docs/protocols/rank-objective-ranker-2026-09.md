# 사전등록 — 시행 L: 순위 목적 GBM 랭커 (2026-09-03, 정식 시행 · family `ranker` · 1회)

진단 D1~D7(pooled-ranker-2026-09.md)이 말한 것: 랭커가 진 원인은 **목적함수**였다(수익률 MSE ≠ 일별 순위 IC). D7 은
진단이라 채택 근거가 아니다 — 여기서 **같은 설정을 고정하고 한 번** 잰다. 통과하면 `ranker` Analyst(관찰 모드)로 배선.

## 고정
- 데이터: pooled-ranker 와 동일 — Analyst 점수 6개(chart·event·flow·fundamental·regime·risk) + 시장 원핫. 국장 271세션 +
  미장 508세션. **타깃·피처 전부 세션×시장 안 순위 정규분위(rank-gauss)**, 결측은 0. 홀드아웃(2026-07~) 안 연다.
- 모델: LightGBM 잎 7 · min_data 2,000 · lr 0.03 · 300 라운드 · bagging 0.8 · feature_fraction 1.0 · L2 1.0 · seed 0.
  학습 = 국장+미장 합침(GBM-POOL). 시드 앙상블 없음(하나로 잰다).
- 워크포워드: 국장 판정 블록 = 랭커 B 와 동일(확장창 ≥150, 20세션 예측, 퍼지 5 → 5블록 100세션). 미장은 같은 날짜 블록.

## 채택 기준 (국장 판정 블록) — 넷 다 통과해야 채택
① ΔIC(h5) vs fundamental 의 일별 차이 NW t ≥ 2.0 ② 상위 24 동일가중 h5 z-수익 ≥ 대조 ③ 최악 20세션 블록 ΔIC ≥ −0.03
④ 미장 판정 블록 ΔIC vs fundamental ≥ 0 (다른 시장에서도 안 지는가).
함께 적되 기준 아님: 피처 gain 순위, 시장별 IC, 상위24 (③②는 꼬리 지표라는 것을 안다 — 그래도 등록된 기준이므로 그대로 잰다).

## 채택 시
`analysts/ranker.py`(관찰 모드, 가중치 0)로 배선 → `analyst_weights` 한계기여 규칙이 가중을 정한다 → shadow → 모의계좌.
랭커는 fundamental 을 **대체**하는 알파다(risk 제약은 그대로). 기각이면 이 분기엔 랭커를 다시 열지 않는다.

## 결과 (2026-09-03 11:21, 해시 f11086e7fe081b68, research_trials `rank-objective-ranker-2026-09:L`) — **채택**

국장 759,051행 + 미장 2,900,177행 학습, 판정 5블록.

| | 랭커 | fundamental | Δ |
|---|---|---|---|
| 국장 100세션 IC(h5) | +0.1052 (t 6.23) | +0.0772 (t 4.76) | **+0.0280 · NW t 2.88** ① ○ |
| 상위24 h5 z-수익 | +0.1687 | +0.1428 | ② ○ |
| 블록별 ΔIC | +0.059 +0.028 +0.003 +0.030 +0.020 | | 최악 +0.003 ③ ○ |
| 미장 104세션 IC | +0.0795 | +0.0485 | **+0.0310 · NW t 4.40** ④ ○ |

피처 gain: risk 51% · flow 15% · chart 11% · fundamental 9% · event 6% · is_us 4% · regime 4%.
D7 진단과 같은 숫자다(같은 설정이니 당연하다 — 이 시행의 뜻은 "회차를 걸고 그대로 잰다"). 미장에서 더 크게 이긴 것은
미장 fundamental 이 약해서(+0.049)이고, 랭커가 두 시장에서 다 +0.08~0.10 이란 건 **점수 결합 자체가 시장 공통**이라는 뜻.

다음: `analysts/ranker.py` 관찰 모드 배선 (별도 문서 `docs/design/ranker.md`).

## 배선 뒤 IC 관문 (2026-09-03 12:35, `tools/measure_ic.py --analyst ranker`, 월말 모델 열 워크포워드)

| 시장 | IC | 표본 | 폴드별 |
|---|---|---|---|
| KR | **+0.0986** (합격선 0.03) | 279일 / 781,616행 | +0.081 +0.115 +0.095 +0.098 +0.105 |
| US | **+0.0609** | 228일 / 1,403,296행 | −0.024 +0.102 +0.113 +0.024 +0.090 |

시행 L 의 숫자와 같은 결이다(국장 0.105 → 0.099, 미장 0.080 → 0.061 — 관문은 정정본·명단 필터를 거친 실전 경로라 조금 낮다).
미장 첫 폴드(2025-08~10)가 음수인 것은 그 구간 모델에 미장 학습 세션이 11~27개뿐이었기 때문이다.
모델 열: `ranker-v0.1.0-{20250630…20260531,20260630}` 13개, as_of 마다 usable_from ≤ as_of 인 최신 것.
다음: 전 Analyst 한계기여 재측정(`--save`) → analyst_weights 갱신 → 후보 선정에 반영.
