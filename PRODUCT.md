# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

운용자 본인 한 사람. 멀티에이전트 AI 사모펀드 **Quant_RL_Trading** 을 만들고 굴리는
개인 퀀트 개발자가, 자기 돈(현재는 모의계좌 5억)을 맡긴 시스템이 지금 무엇을 하고
있는지 확인하고 개입 여부를 판단한다. 외부 관객은 없다 — 설득할 사람이 없으므로
화면은 마케팅이 아니라 계기판이다.

## Product Purpose

시장보다 덜 잃고 시장보다 더 버는 것. 한 숫자로는 **정보비율(IR)**.
대시보드의 역할은 "얼마 벌었나"가 아니라 **"리스크 예산을 얼마나 쓰고 있나"** 를
먼저 답하는 것이다 (docs/design/dashboard.md §1 — 리스크 예산 화면).
성공은: 운용자가 한 화면에서 ① 포트폴리오 상태 ② 모델 학습 건전성 ③ 데이터 신뢰
④ 다음 일정을 판단하고, 이상 징후(액션 반영률 저하, 낙폭 밴드 진입, 수집 실패)를
놓치지 않는 것.

## Positioning

이웃 제품(증권사 MTS, 일반 포트폴리오 트래커)이 흉내 낼 수 없는 것:
- **이중시간(as_of) 타임머신** — 모든 숫자가 "그때 알 수 있었던 것"으로 재생된다.
  모든 API 가 `as_of` 를 받는다 (불변식 9).
- **AI 관여의 투명한 추적** — RL·지도·비지도·LLM 각각이 오늘 매매에 실제로 얼마나
  개입했는지(액션 반영률)를 상시 표시한다. 30% 미만이면 룰 시스템으로 전락한 것.
- 백테스트와 라이브가 같은 코드라는 사실을 화면이 증명한다 (장부 vs 계좌 대조 패널).

## Operating Context

- **기기 반반**: 장중 수시 확인은 휴대폰(port 5059, LTE), 연구·분석은 데스크톱
  1920×1080. **둘 다 1급 기준** — docs 의 "데스크톱 한 화면" 규칙과 모바일 밀도가
  동등하게 지켜져야 한다. (2026-08-31 확인: 기존 docs 는 데스크톱만 1급이었다)
- 하루 리듬: 08:40 세션 주문 → 장중 20분 추격 → 15:20 취소 → 15:45 대사 →
  15:55 수집 → 16:10 run_daily → 밤 연구(IC·학습). 화면은 이 리듬의 각 단계가
  돌았는지/실패했는지를 보여줘야 한다.
- WSL2 + 9.7GB 머신에서 연구 작업과 동거 — 화면 프로세스는 메모리 상한(512MB)을
  지켜야 한다 (2026-08-31: 화면 5개가 스왑 지옥의 범인이었다).

## Capabilities and Constraints

- 스택: Flask + vanilla JS + **ECharts(유일 허용 차트 라이브러리)**, 빌드 도구 없음.
- 탭: 트레이딩 / 마켓 / 학습 / 시스템 / 더보기(뉴스·일정 등). 목록은 패널 안 스크롤,
  sticky 머리글. 탭 전환 시 `as_of` 유지.
- 금지: `localStorage`/`sessionStorage`, 하드코딩 임계치(전부 `store.config`),
  ECharts 외 차트 라이브러리, 시크릿 노출.
- 모든 숫자에 시점(`as_of`)이 붙는다. 기준일 없는 데이터 표시는 사용자 최우선 불만
  이력이 있다 (memory: data-must-be-dated).
- 언어: 한국어 단일.

## Brand Commitments

공식 명칭 **Quant_RL_Trading** (2026-08-31 확정) — 화면·문서에 이대로 쓴다.
시각 기준 참조물: `docs/design/fund-reference.html` (색·타이포·밀도의 기준, 구현물 아님).

## Evidence on Hand

- 실데이터 전부 실물: 모의계좌 장부(data/_paper), 창고(data/), IC 측정 이력,
  워크포워드 성적, §7 베이스라인 비교. **지어낼 필요도, 지어내서도 안 된다** —
  화면의 모든 숫자는 창고에서 온다 (불변식 1).
- 설계 문서: docs/design/dashboard.md (3탭 명세·밀도 규칙·API 규약) 외 docs/design/*.

## Product Principles

1. **판단이 한 화면에서 끝난다** — 고밀도, 스크롤은 보조 정보에만.
2. **모든 숫자는 추적 가능하다** — 왜 이 값인지 파고들어지고, 시점이 붙는다.
3. **침묵하는 실패를 화면이 고발한다** — 수집 누락·대사 실패·액션 반영률 저하는
   빈칸이 아니라 경고로 보인다.
4. **휴대폰과 데스크톱은 동급이다** — 축약판이 아니라 같은 판단이 가능한 화면.
5. **화면은 가볍다** — 운용 머신과 동거하므로 메모리·요청 비용이 설계 제약이다.
