# LS 모의투자 계좌 배선 (2026-08-22)
#
# 조각으로 나눠 심는다. `quant_rl_trading.yaml` 전체를 심으려면 그때까지
# 쌓인 모든 변경에 발효 시점을 줘야 하는데, 그건 이 작업과 무관한 값들까지
# 한 번에 바꾸는 일이다. 새 키만 따로 심는다 (`config/rl_cache.yaml` 과 같은 방식).
#
# **본 파일(`quant_rl_trading.yaml`)에도 같은 값이 적혀 있다** — 거기가 단일
# 소스이고 이건 심기용 조각이다. 두 곳이 갈라지면 본 파일이 옳다.
execution:
  # 전송 스위치. 모의 계좌로 켠다 — 본 파일의 주석 참조.
  live_trading: true
  account_mode: paper
  live_account_fingerprint_paper: "164cccf81af0"
