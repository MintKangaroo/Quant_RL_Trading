# M3 — Selector + Executor 착수 프롬프트

**여기서 이미 돈을 벌 수 있어야 한다.** RL 없이 실전 운용이 가능한 상태를 만든다.
`docs/design/selector.md` 와 `docs/design/accounting.md` 를 먼저 읽힌다.

---

## 3-1. 회계 계층 ⭐ 가장 먼저

```
docs/design/accounting.md 를 읽고 quant_rl_trading/accounting/ 을 구현할 계획을 세워줘.

보상 함수의 r_port 가 여기서 정의돼. 이게 틀리면 위에 뭘 쌓아도 틀려.

핵심:
- NAV = KRW현금 + USD현금×FX + 국내주식평가 + 해외주식평가×FX
        + 미수배당(세후) − 미지급 수수료·세금
- 발생주의(trade-date). 결제일 기준 아님
- 일간 스냅샷은 한국시간 15:40 하루 한 번. 미장은 직전 종가 사용
- 벤치마크도 정확히 같은 시각·같은 규칙으로 계산 (다르면 가짜 초과수익이 생겨)
- 배당은 배당락일에 세후로 계상. 입금일 인식 금지
  (입금일 기준이면 배당락 하락을 보상 함수가 실제 낙폭으로 오인해)
- 해외 양도세는 일간 NAV 에 넣지 말고 별도 충당금. 세전/세후 둘 다 보고
- 수익률은 TWR. 입금일에 수익이 난 것으로 오인하면 안 돼
- 낙폭은 NAV 원금액이 아니라 TWR 누적지수로 계산
  (원금액으로 하면 입금이 낙폭을 지워서 MDD 예산이 무의미해져)
- 벤치마크는 총수익지수(TR). 가격지수면 배당수익률만큼 가짜 알파가 생겨

NAV 산출은 이 모듈에서만 해. Executor·Auditor·dashboard·reporting 이
각자 계산하면 반드시 어긋나.

테스트:
- 입금일 TWR 수익률이 0인지
- 배당락일에 낙폭이 발생하지 않는지
- 환율만 변동한 날 해외 포지션이 정확히 그만큼 움직이는지
- 매수 직후 수수료만큼 NAV 가 감소하는지
```

---

## 3-2. Selector — 합성과 후보 선정

```
docs/design/selector.md 를 읽고 quant_rl_trading/selector/ 를 구현할 계획을 세워줘.
진화는 아직. 먼저 고정 가중치로 동작하는 합성·선정 로직을 만들어.

combined = Σ(wᵢ·scoreᵢ·confᵢ) / Σ(wᵢ·confᵢ)

선정 순서 (순서가 중요해):
1. 유니버스 필터 (거래대금 하한 / 관리종목·거래정지 제외 /
   상장 6개월 경과 / 1주 가격 < 자본의 15%)
2. combined score 계산 — 죽은 Analyst 는 중립 0
3. News·SNS 거부 종목 제외 (거부 상한 30%)
4. 상관 페널티 — 기선정 종목과 60일 상관 > 임계 시 감점
5. 섹터 상한
6. 상위 N개

3번을 4번보다 먼저 둬. 상관 계산은 비싼데 거부될 종목까지 계산할 이유가 없어.

초기 가중치는 동일가중으로. 진화는 3-4 에서.
```

---

## 3-3. Executor

```
quant_rl_trading/executor/ 를 구현할 계획을 세워줘.
agents.md §7 의 실행 순서를 그대로 지켜. 순서가 곧 안전이야.

1. 킬스위치 latch 확인 → 걸려 있으면 즉시 종료
2. 데이터 품질 게이트
3. 시장 서킷 브레이커 (KOSPI/KOSDAQ 분리)
4. defer 게이트 (개장 후 30분 신규매수 보류)
5. 목표비중 → 주식 수 변환 (정수 라운딩 / 최소금액 / 고가주 제외)
6. 3% 거래대금 상한, 청산 3일 제약
7. 주문 분할 집행
8. 실현 비중 기록 → Allocator 되먹임  ← 절대 생략 금지

주문 처리:
- 기본은 지정가. 슬리피지 상한을 넘으면 주문하지 않음
- 시장가는 청산과 킬스위치 발동 시에만
- 분할 집행: 슬라이스 수와 간격을 config 로
- 미체결은 N분 후 재호가, 최대 재시도 횟수 제한
- 부분체결을 정상 경로로 처리

멱등성 ⭐:
client order id 를 (session_id, entity_id, slice_seq) 로 결정론적으로 생성해서
재시작 시 중복 주문이 나가지 않게 해줘. 프로세스가 죽었다 살아나는 건
반드시 일어나는 일이야.

Executor 안에는 AI 가 없어. 순수 코드만 (불변식 6).

킬스위치:
- latch 방식. 한번 걸리면 사람이 수동으로 풀기 전까지 유지
- 발동 조건: 낙폭 30% 초과 / 주문 실패율 임계 초과 / 데이터 게이트 실패
- 발동 시 전 포지션 청산 여부는 config 로 (기본: 신규매수만 차단)
```

---

## 3-4. 룰 베이스라인 + 진화

```
Allocator 자리에 룰 베이스라인을 넣어줘. RL 은 M4 야.

베이스라인 3종을 전부 구현하고 config 로 전환 가능하게:
1. 동일가중
2. 스코어 비례 ← 기본값. M4 에서 RL 의 진짜 경쟁자가 될 것
3. 스코어 비례 + 변동성 역가중

그 다음 selector.md §4 의 진화 알고리즘을 구현해줘:

population 64, generations 40, tournament k=3, SBX(eta=15) 0.9,
gaussian mutation sigma=0.05 rate 0.2, elitism 2

적합도 = IR(검증구간) − λ_L1·Σ|wᵢ| − λ_turn·회전율
- IR 로 평가해. 수익률로 하면 리스크를 태운 개체가 이겨
- L1 페널티가 핵심이야. 쓸모없는 Analyst 가중치를 0 으로 밀어
- 평가는 실제 백테스트로 (비용·라운딩 포함). 점수 상관만으로 하면
  실제로 못 사는 종목으로 적합도를 얻어

과적합 방지:
- 매 세대 검증 구간을 다른 폴드로 리샘플링
- 개체당 여러 시작일 백테스트의 중앙값으로 평가
- 조기 종료 (10세대 개선 없으면)
- IC 0.03 미통과 Analyst 는 유전자에서 제외하고 w=0 고정
- 국장/미장 별도 개체군

안정성 검사를 반드시 넣어줘:
상위 10개 개체의 가중치가 서로 비슷한지 확인해서 보고해.
제각각이면 적합도 지형이 평평하다는 뜻이고, 그 진화 결과는 노이즈야.
그 경우 동일가중을 쓰는 게 낫다고 솔직하게 말해줘.
```

---

## 3-5. Session 파이프라인

```
하루의 의사결정 사이클을 하나의 Session 으로 묶어줘.

국장:
07:00 Collector — 미장 마감·환율·야간 뉴스
08:00 store — universe 스냅샷 + 데이터 품질 게이트
08:30 Analyst 9명 병렬 → Signal 생성
08:50 Selector — 후보 선정
08:55 Allocator(룰) — 목표 비중
09:00 개장, defer 게이트
09:30 Executor — 주문 집행
장중   Chart·Flow 5분 갱신, 리스크 가드 상시
15:20 종가 리밸런싱
15:40 회계 스냅샷 (NAV, TWR, 낙폭)

미장은 한국시간 22:30~05:00 (서머타임 23:30~06:00) 동일 구조.
주 1회 금요일 마감 후 KR/US 배분 재계산 (데드밴드 10%p).

전 단계를 이벤트 로그에 append-only 로 기록해줘.
같은 as_of 로 리플레이하면 동일한 주문이 나와야 해.
```

---

## 3-6. 트레이딩 탭

```
dashboard-kickoff.md 의 D-3 를 실행해줘.
Decision Trace 모달까지 포함.
```

---

## 3-7. shadow 운용

```
실전 투입 전 shadow 를 붙여줘.

- 10거래일간 실제 주문 없이 결정만 기록
- 매일 결정·예상 체결·예상 손익을 저장
- 데이터 게이트 실패, Analyst 장애, 주문 변환 실패 건수를 집계

무사고 통과 후 실전 소액 투입.
소액 구간은 전략 검증이 아니라 배선 검증이야 (accounting.md, reward-and-risk.md §7).
수익률로 전략을 판단하지 마.
```

---

## 3-8. M3 완료 확인

```
docs/milestones.md 의 M3 완료 기준을 검증하고 증거를 보여줘.

추가로 확인:
- 같은 as_of 로 Session 을 두 번 돌렸을 때 주문이 바이트 단위로 동일한지
- 프로세스를 강제 종료했다 재시작했을 때 중복 주문이 나가지 않는지 (멱등성)
- 킬스위치를 실제로 발동시켰을 때 신규매수가 차단되는지
- 실현 비중이 기록되고 있는지 (M4 에서 되먹임에 쓰일 값)
```
